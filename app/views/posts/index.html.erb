<% if @user %>
  <h1>Here is a list of posts from <%= @user.name %></h1>
  <ul>
  <% @posts.each do |post| %>
    <li><%= link_to post.title, user_post_path(@user, post) %></li>
  <% end %>
</ul>
<% else %>
  <p>User not found</p>
<% end %>