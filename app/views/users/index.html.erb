<div class="flex flex-col">
  <p style="color: green"><%= notice %></p>

<h1>Users</h1>

<ul class="flex gap-4 flex-col w-100 h-100 border">
  <% @users.each do |user| %>
  
    <%= render user %>
  <% end %>

</ul>
</div>